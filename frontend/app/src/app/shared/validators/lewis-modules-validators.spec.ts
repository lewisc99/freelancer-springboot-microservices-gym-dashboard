import { LewisModulesValidators } from './lewis-modules-validators';

describe('LewisShopValidators', () => {
  it('should create an instance', () => {
    expect(new LewisModulesValidators()).toBeTruthy();
  });
});